# tinacms-authjs's peer dependency range hasn't been updated to include
# Next.js 16 yet, even though the code it generates (a classic Pages
# Router API handler) is unaffected by Next 16's changes. Without this,
# `npm install` fails outright wherever peer deps are enforced strictly
# (e.g. Vercel's build).
legacy-peer-deps=true
